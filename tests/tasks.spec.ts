import { test, expect } from "@playwright/test";
/*
test('deve poder cadastrar uma nova tarefa', async ({ page }) =>{
    await page.goto('http://localhost:3000');

    const inputTaskName = page.locator('input[class*=InputNewTask]')
    await inputTaskName.fill('Task 1');
    // await page.fill('#newTask', 'Task 1');
    // await page.fill('input[placeholder="Add a new Task"]', 'Task 1');
    // await page.fill('input[type=text]', 'Task 1');

    // Valores '_1y0mp_21' certamente são gerados dinamicamente
    // await page.fill('._listInputNewTask_1y0mp_21', 'Task 1');

    // Alteração com um 'regex' para pegar apenas a parte 'fixa' do class
    await page.fill('input[class*=InputNewTask]', 'Task 1');


})*/